# Andy'K Landing Page — MVP PRD (AI-Actionable)

**Project:** andyk-landing
**Stack:** Next.js 16 + React 19 + TypeScript 5 + Tailwind CSS v4
**Design Reference:** `docs/DESIGN_PLAN.md`
**Task Board:** `docs/KANBAN.md`

---

## Current State

The landing page is a single-page Next.js app at `/` with 14 active components. It has:
- Hero with 2 CTAs
- Scrolling top banner
- Navbar with hash navigation
- 6-step A.D.A.M. roadmap pipeline visualization
- Founders section (CEO + CTO with photos, bios, LinkedIn)
- 3-tab pricing section (B2B / B2G / Tech — 13 plans total)
- Social proof section (stats: 150+ campaigns, etc.)
- FAQ accordion
- Integrations section
- Dark-bg CTA section
- Footer with links + company info

### What's Missing
1. `/onboarding` route — referenced by 6 CTAs, does not exist
2. Contact form — all CTAs are mailto links
3. SEO metadata — no Open Graph, no Twitter cards, no structured data
4. Image optimization — co-founder.jpg is 103KB unoptimized
5. Dead code — 5 unused Mock components
6. Analytics — no tracking at all
7. Playwright tests — only 1 example test exists
8. Sitemap + robots.txt — missing for SEO

---

## Feature Specs

### F1. Contact Form Component

**Priority:** HIGH — replaces all mailto links with a real form

**Requirements:**
- React server action or API route for form submission
- Fields: Name, Email, Company, Service Interest (dropdown: B2B/B2G/Tech/A.D.A.M. License), Message
- Email validation (company-domain preferred, public allowed for landing page)
- Success/error states with clear feedback
- Mobile: full-width inputs, 44px height, sticky submit button
- Desktop: 2-column layout (info left, form right)
- Send email via EmailJS or Resend API (configurable)
- Honeypot + rate limiting (no captcha)
- Add to CTA section and as standalone `#contact` section

**Design:**
- White card on dark background (CTA section)
- Inputs: `border-grid-500`, `focus:border-highlight`, rounded-lg
- Submit button: `bg-highlight text-white` or `btn-primary-gradient`
- Success message: highlight-colored with checkmark

**Data model addition to `data.ts`:**
```typescript
export const CONTACT_FORM = {
  heading: "Let's Talk",
  subheading: "Tell us about your business and we'll show you how we can help.",
  serviceOptions: [
    "B2B Business Development",
    "B2G Public Sector Advisory",
    "Technology / CTO Services",
    "A.D.A.M. License Inquiry",
    "Partnership",
    "Other",
  ],
};
```

---

### F2. `/onboarding` Route

**Priority:** HIGH — 6 CTAs point here, returns 404

**Requirements:**
- New route at `src/app/onboarding/page.tsx`
- Simple landing page that explains what happens next
- Embedded questionnaire or link to external questionnaire
- Phase 1: Static page with "Coming Soon" + contact form fallback
- Phase 2: Typeform-style questionnaire (when A.D.A.M. backend is ready)

**Design:**
- Same layout as homepage but simplified (Navbar + content + Footer)
- Hero area with "Tell Us About Your Business" heading
- 3 cards showing the 3 service types (B2B/B2G/Tech) with brief descriptions
- Each card links to mailto with pre-filled subject OR future questionnaire
- Bottom CTA: "Not sure? Contact us directly" → contact form

**Route structure:**
```
src/app/onboarding/
  page.tsx       # Main onboarding landing
  layout.tsx     # Optional: shared layout with Navbar/Footer
```

---

### F3. SEO & Meta Enhancement

**Priority:** MEDIUM — needed before deployment

**Requirements:**
- Open Graph meta tags (title, description, image, url, type)
- Twitter Card meta tags (summary_large_image)
- JSON-LD structured data (Organization + WebSite)
- Dynamic `<title>` per route (homepage vs onboarding)
- Canonical URLs
- `sitemap.xml` generated by Next.js
- `robots.txt` with sitemap reference

**Implementation in `layout.tsx`:**
```typescript
export const metadata: Metadata = {
  metadataBase: new URL("https://andykgroup.com"),
  title: {
    default: "Andy'K Group International | B2B & B2G Business Automation",
    template: "%s | Andy'K Group International",
  },
  description: "Strategic partner in business automation & outreach...",
  openGraph: {
    type: "website",
    locale: "en_GB",
    siteName: "Andy'K Group International",
    images: [{ url: "/og-image.png", width: 1200, height: 630 }],
  },
  twitter: {
    card: "summary_large_image",
  },
};
```

**Create:**
- `public/og-image.png` — 1200x630 branded Open Graph image
- `src/app/sitemap.ts` — Next.js sitemap generator
- `src/app/robots.ts` — Next.js robots.txt generator

---

### F4. Image Optimization

**Priority:** MEDIUM

**Requirements:**
- Convert `co-founder.jpg` (103KB) to WebP, resize to max 400px width
- Use `next/image` component for all images (auto optimization)
- Add explicit `width`, `height`, `alt` to all `<img>` tags
- Consider self-hosting logo images instead of Supabase CDN (reduce external dependencies)
- Create `public/images/` directory for organized assets

---

### F5. Dead Code Cleanup

**Priority:** LOW — housekeeping

**Files to remove:**
- `src/components/MockAgentPanel.tsx`
- `src/components/MockContextStudio.tsx`
- `src/components/MockDashboard.tsx`
- `src/components/MockNotebook.tsx`
- `src/components/MockSlack.tsx`

**Verify first:** grep for imports to confirm these are truly unused.

---

### F6. Analytics Setup

**Priority:** LOW — needed for marketing decisions

**Requirements:**
- Umami Analytics (self-hosted, privacy-friendly) OR Plausible
- Track: page views, CTA clicks, pricing tab switches, FAQ opens
- No cookies, GDPR-compliant by default
- Script loaded via `next/script` with `strategy="afterInteractive"`

---

### F7. Playwright E2E Tests

**Priority:** LOW — quality assurance

**Requirements:**
- Test homepage loads and all sections are visible
- Test navigation links scroll to correct sections
- Test pricing tab switching works
- Test FAQ accordion expand/collapse
- Test mobile responsive layout (375px viewport)
- Test `/onboarding` route loads (when implemented)
- CI: Run on GitHub Actions (already configured in `.github/`)

---

### F8. Founders Section Redesign

**Priority:** MEDIUM — per instructions.md

**Requirements:**
- New layout style (side-by-side cards, not stacked)
- Keep existing data from `FOUNDERS` in `data.ts`
- Photos with grayscale → color hover effect
- LinkedIn + email icon links
- Quote in italics below bio
- Location badge
- Mobile: stacked cards, full-width
- Desktop: 2 equal columns with card styling

---

### F9. Roadmap Section Enhancement

**Priority:** MEDIUM — per instructions.md

**Requirements:**
- Replace static showcase images with arrow-connected step flow
- 6 steps from `ADAM_ROADMAP` in `data.ts`: Onboarding → Strategy → Contract → Invoice → Follow-up → Dashboard
- Visual: numbered circles connected by arrows/lines
- Each step: icon + title + short description
- Mobile: vertical flow with connecting line
- Desktop: horizontal flow or 2-row grid with arrows
- Clear visual progression (step 1 → step 6)

---

### F10. Content Sync from Corporate Site

**Priority:** LOW

**Requirements:**
- Review content from `/home/kobe/Documents/andyk/andykgroup.com/`
- Ensure pricing, services, and company info are consistent
- Corporate site is a Base44-generated SPA — extract any missing content
- Key: ensure `data.ts` has all services, pricing matches instructions.md

**Pricing Discrepancy Check:**
| Plan | instructions.md | data.ts | Status |
|------|----------------|---------|--------|
| B2B Core | €950 | €950 | OK |
| B2B Advance | €1,350 | €1,350 | OK |
| B2B Vanguard | €1,750 | €1,750 | OK |
| B2B Prestige | from €2,400 | €2,400 | OK |
| B2G GovStarter | £650 | £650 | OK |
| B2G GovExpand | £1,050 | £1,050 | OK |
| B2G GovElite | £1,650 | £1,650 | OK |
| Tech Architecture | from €2,500 | €2,000 | MISMATCH |
| Tech Platforms | from €4,000 | €3,000 | MISMATCH |
| Tech Automation | from €2,000 | €1,500 | MISMATCH |
| Tech Audit | from €2,800 | €1,800 | MISMATCH |
| Tech CTO-as-a-Service | from €100/hr | €2,500/mo | DIFFERENT UNIT |

**Action:** Verify correct pricing with CEO (Andrej) before updating. The `instructions.md` prices may be the authoritative source.

---

## Non-Functional Requirements

| Requirement | Target |
|-------------|--------|
| Lighthouse Performance | > 95 |
| Lighthouse Accessibility | > 90 |
| First Contentful Paint | < 1.0s |
| Largest Contentful Paint | < 2.0s |
| Cumulative Layout Shift | < 0.1 |
| Mobile usability | Full functionality at 375px |
| Browser support | Chrome, Firefox, Safari, Edge (latest 2 versions) |

---

*This PRD is the spec authority. See KANBAN.md for execution order.*
